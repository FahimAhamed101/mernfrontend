{
    "buildCommand": "cd reactshop && npm install && npm run build",
    "outputDirectory": "reactshop/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "server.js"
        }
    ]
}